export const queryClientOptions = {
  defaultOptions: {
    queries: {
      refetchOnWindowFocus: false
    }
  }
}

export const ReactQueryKeys = {
  LOBBY: ['lobby'],
  PLAYER: ['player'],
  GAME: ['game'],
}